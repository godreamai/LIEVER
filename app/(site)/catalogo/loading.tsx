export default function Loading() {
  return (
    <div className="wrap center" style={{ paddingTop: 60, paddingBottom: 60 }}>
      <p style={{ fontFamily: "var(--font-body)", fontSize: 14, color: "var(--text-muted)" }}>Cargando catálogo…</p>
    </div>
  );
}
